# Domains
One subdirectory per bounded context.
Each domain contains a knowledge-base.md with non-derivable domain context.

## Structure
specs/domains/{domain-name}/
  knowledge-base.md    ← Tier 2 knowledge: domain-level context
