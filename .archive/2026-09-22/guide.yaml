# System Guide — Where to look when a feature or issue needs analysis.
# Auto-generated by sk.design. Manual edits to also-check fields are preserved.
# Per-entry limit: max 3 lines (summary + code + also-check).

tier: system
last-updated: null

# Populate during sk.init or first sk.design run.
# One entry per domain/service.
#
# domains:
#   {domain-name}:
#     summary: "What this domain owns."
#     code: "{source path}"
#     also-check: "Non-obvious cross-cutting concerns."
