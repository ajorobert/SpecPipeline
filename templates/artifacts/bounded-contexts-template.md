# Bounded contexts
The index of `specs/domain/`. One row per context; the context's own file holds its detail.

| Context | File | Owns | Code |
|---|---|---|---|
| {Module} | [{module}.md]({module}.md) | {one line} | `{code path}` |

## Context map
<!-- One line per relation: upstream → downstream, relation type, and the contract that carries it. -->
- {Upstream} → {Downstream}: {customer/supplier | conformist | published language | ACL} via {contract}
