# Projects
The project router. One row per deployable project in this repository; a single-project repository has
one row. Per-project phases read Type and Code Root from here (via `unit-brief.md` → Impacted Projects).

| Project | Type | Code Root | Role |
|---|---|---|---|
| [{Project}]({Project}/tech-stack.md) | {Backend \| Frontend \| Mobile} | `{path}` | {what it serves, e.g. "admin console for operators"} |
