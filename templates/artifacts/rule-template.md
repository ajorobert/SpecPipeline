---
paths:
  - "{glob of the files this rule governs, e.g. src/backend/**/Endpoints/**/*.cs}"
---
# {Topic}
<!-- Self-contained: an engineer or an AI can apply it with nothing else open. Under 200 lines.
     No @imports. Cite decisions as provenance only. Each line is something a reviewer can check. -->

- {MUST / MUST NOT …}
- {…}

Source: ADR-{NNNN}
