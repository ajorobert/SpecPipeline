# Constitution
<!-- PROJECT-OWNED. Written by /sk.init from what the team states; amended by re-running /sk.init → constitution.

     Holds only the project's FIXED core principles: content that does not change as the system evolves.
     The framework proposes none — every line here was stated by the team.
     - No versions, module lists, tooling or status (those live in projects/{P}/tech-stack.md and the code).
     - No rules that a single ADR or a .claude/rules/ file already carries; reference the ADR instead
       ("Contracts are the source of truth — ADR-0014").
     - Each principle is declarative and checkable.
     It outranks every other source (.claude/skills/governance/profile.md → Precedence);
     sk.design and sk.review treat a contradiction with it as BLOCKING. -->

## Principles
1. {principle} — {ADR-NNNN, if one elaborates it}
